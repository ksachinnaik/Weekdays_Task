import React from 'react';
import Button from '@material-ui/core/Button';

const ApplyButton = () => {
  return <Button variant="contained" color="primary" style={{ marginTop: 10 }}>Apply</Button>;
};

export default ApplyButton;
